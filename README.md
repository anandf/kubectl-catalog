# kubectl-catalog
A kubectl plugin to work with OLM catalog images on any Kubernetes cluster without the need of OLM. It will help to install OLM bundles either from community bundles or from Red Hat registry without the need for OLM being installed in a plain vanila Kubernetes cluster.
